# Local Mount Configuration
# NFS volume is pre-mounted to /mnt/media in the VM
MEDIA_PATH=/mnt/media

# Note: VPN/Gluetun removed - qBittorrent runs without VPN protection
# You can configure VPN directly in qBittorrent settings if needed